# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Node (por si en el futuro agregamos build tools)
node_modules/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Local env / secrets
.env
.env.*
!.env.example

# Cloudflare Wrangler (si se usa CLI más adelante)
.wrangler/
wrangler.toml.local

# Build outputs (defensivo — hoy no hay build)
dist/
build/
.cache/
